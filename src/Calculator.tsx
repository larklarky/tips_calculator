import { Form } from "./Form"
import {Results} from './Results';

export const Calculator = () => {
    return(
        <div className="container">
            <Form/>
            <Results/>
            
        </div>
    )
}